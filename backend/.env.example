# Example Environment Variables for the HopeHands Backend

# MongoDB Connection String
# Replace this with your actual MongoDB Atlas or local MongoDB connection string.
MONGO_URI=mongodb://localhost:27017/hopehands

# JSON Web Token Secret
# This is a secret key for signing tokens. Use a long, random string in production.
JWT_SECRET=your_jwt_secret_key_here

# Server Port
# The port the Express server will run on.
PORT=5000
